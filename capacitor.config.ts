import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.calvinsteck.ridingtracker',
  appName: 'Riding Tracker',
  webDir: 'dist'
};

export default config;
